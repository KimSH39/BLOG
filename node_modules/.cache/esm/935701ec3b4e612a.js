let Joi,check,Admin;_3b4‍.x([["register",()=>register],["login",()=>login],["logout",()=>logout]]);_3b4‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_3b4‍.w("prettier",[["check",["check"],function(v){check=v}]]);_3b4‍.w("../../model/admin",[["default",["Admin"],function(v){Admin=v}]]);



       const register = async ctx => {
  const schema = Joi.object.keys({
    name: Joi.string().min(2).required(),
    email: Joi.string.email().required(),
    password: Joi.string().min(8).required(),
  });

  const result = schema.validate(ctx.request.body);

  if (result.error) {
    ctx.status = 400;
    ctx.body = result.error;
    return;
  }

  const { name, email, password } = ctx.request.body;

  try { 
    const emailExist = await Admin.findOne({ email });

    if (emailExist) {
      ctx.status = 409;
      return;
    }
    const admin = new Admin({
      name,
      email,
      password,
    });

    await admin.setPassword(password);
    await admin.save();

    ctx.body = admin.serialize();
  } catch (e) {
    ctx.throw(500, e);
  }
};

       const login = async ctx => {    
  const schema = Joi.object().keys({
    email: Joi.string.email().required(),
    password: Joi.string().min(8).required(),
  });

  const result = schema.validate(ctx.request.body);
  if (result.error) {
    ctx.status = 400;
    ctx.body = result.error;
    return;
  }

  const { email, password } = ctx.request.body;

  try {
    const admin = await Admin.findOne({ email });
    const valid = await admin.checkPassword(password);

    if (!admin || !valid) {
      ctx.status = 401;
      return;
    }

    ctx.body = admin.serialize();
  } catch (e) {
    ctx.throw(500, e);
  }
};

       const logout = async ctx => {


};