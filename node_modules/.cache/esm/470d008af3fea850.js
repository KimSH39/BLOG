let Koa,dotenv;_f3f‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_f3f‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();

const { POST } = process.env;

const app = new Koa();

app.use(ctx => {
    ctx.body = 'hellp koa';
});

const port = PORT || 4000;
app.listen(port, () => {
    console.log(`Listening to port ${port}`);
});